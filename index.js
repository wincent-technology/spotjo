/**
 * @format
 */
import "react-native-gesture-handler";

import { AppRegistry } from 'react-native';
import App from './App';
import { name as appName } from './app.json';
import { share } from "./src/IconManager";

// const modules = require.getModules();
// const moduleIds = Object.keys(modules);
// const loadedModuleNames = moduleIds
//   .filter((moduleId) => modules[moduleId].isInitialized)
//   .map((moduleId) => modules[moduleId].verboseName);
// const waitingModuleNames = moduleIds
//   .filter((moduleId) => !modules[moduleId].isInitialized)
//   .map((moduleId) => modules[moduleId].verboseName);

// // make sure that the modules you expect to be waiting are actually waiting
// console.log(
//   'loaded:',
//   loadedModuleNames.length,
//   'waiting:',
//   waitingModuleNames.length
// );

// // grab this text blob, and put it in a file named packager/modulePaths.js
// console.log(
//   `module.exports = ${JSON.stringify(
//     loadedModuleNames.sort(),
//     null,
//     2
//   )};`
// );

AppRegistry.registerComponent(appName, () => App);


// 1) also add skills
// 2) clear 
// 3) both are same
// 4) reroute on map


// 2nd screen map
// location enabled



// company flow

// staff not allowed for bul resume / user management

// please validate otp 


// m/w/d in right side //
// whats your skill fontsize small //
// by defult salary = 10k //
// replace function with skills //


// resource profile remove 100% //
// stars let b gray //
// company admin connot able to upload resume //
// salary type 
// job proffession

// bulk resume upload
// please trim the space
// company admin can add another admin
// webview url share
// webview show 

